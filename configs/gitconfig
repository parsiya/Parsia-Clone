#.gitconfig alias
[alias]
	add-dry = add . --dry-run
	cb = checkout -b
	# Add everything and commit. Usage: `git cm "commit msg"`
	cm = !git add . && git commit -m
	# Merge branch into master and squash merge.
	merge-squash = !git checkout master && git merge --squash 
